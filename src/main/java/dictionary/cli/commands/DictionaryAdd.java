package dictionary.cli.commands;

import picocli.CommandLine.Command;

import dictionary.cli.DictionaryCommandline;

@Command(name = "add", description = "Add word")
public class DictionaryAdd implements Runnable {
    @Override
    public void run() {
    }
}
